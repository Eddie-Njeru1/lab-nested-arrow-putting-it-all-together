// Create outer function and initialise login tracking
function createLoginTracker (userInfo) {
  const username = {
    "username": "user"
  };
  const password = {
    "password": "password123"
  };
  let attemptCount = 0;
    

  }
